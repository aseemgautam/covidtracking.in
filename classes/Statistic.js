/* eslint-disable prefer-destructuring */
export default class Statistic {
	constructor(name, primary, secondary, helpText) {
		this.name = name;
		this.primary = primary;
		this.secondary = secondary;
		this.helpText = helpText;
	}
}
